{
    "name": "epochie-monorepo",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "cd server && npm install && cd ../client_new && npm install",
        "build": "cd client_new && npm run build",
        "start": "node server/server.js"
    }
}